<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Material3 theme -->
    <style name="Theme.Restrurant_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional: Set system bar colors if needed -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- If using XML views, you can define fallback colors here -->
        <!-- These are NOT used by Compose themes directly -->
        <!-- <item name="colorPrimary">@color/primary</item> -->
        <!-- <item name="colorSecondary">@color/secondary</item> -->
    </style>

</resources>
